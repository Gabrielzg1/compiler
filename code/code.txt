{este programa e o primeiro teste}
programa teste;
{}
var x,y,total: inteiro;

procedimento soma;
var total, x, y, u: booleano;
inicio
  t:= x+y;
fim;

inicio
  leia(x);
  leia(y);
  total := soma;
  escreva(total)
fim.