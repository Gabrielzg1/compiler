{este programa e o primeiro teste}
programa teste;
{}
var x,y,total: inteiro;

funcao soma: booleano;
var x, y, total: inteiro;
inicio
  total := x + y;
fim;

procedimento soma2;
var x, y, total: inteiro;
inicio
  total := x + y;
fim;



inicio
  leia(x);
  leia(y);
  total := soma;
  escreva(total)
fim.