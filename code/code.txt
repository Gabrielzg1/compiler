{este programa e o primeiro teste}
programa teste;
{}
var x,y,total: inteiro;

funcao soma: booleano;
var x, y, total: inteiro;
inicio
  total := x + y;
  soma := total;
fim;

procedimento soma2;
var x, y, total: inteiro;
inicio
  total := x + y;
fim;



inicio
  leia(x);
  leia(y);
  total := soma + 10 - 50 + soma2;
  escreva(total)
fim.